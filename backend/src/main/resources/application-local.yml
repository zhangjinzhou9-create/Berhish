server:
  port: 8443
  ssl:
    enabled: true
    key-store: classpath:campus-flow-local.p12
    key-store-password: ${TLS_KEY_STORE_PASSWORD:changeit}
    key-store-type: PKCS12
    key-alias: campus-flow-local

app:
  base-url: ${APP_BASE_URL:https://localhost:8443}

